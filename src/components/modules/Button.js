import myButton from '@material-ui/core/Button'

import React from 'react';

const Button = (data) => {
    return (
        <myButton>
            data.title
        </myButton>
    );
}

export default Button;
